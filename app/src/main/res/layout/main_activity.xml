<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout android:id="@+id/drawerLayout"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    tools:openDrawer="start">

    <androidx.coordinatorlayout.widget.CoordinatorLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".MainActivity">

        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginBottom="8dp"
            android:layout_marginTop="8dp"
            app:layout_behavior="@string/appbar_scrolling_view_behavior">

            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/itemsRecycler"
                android:name="it.sephiroth.android.app.appunti.ItemListFragment"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                app:layoutManager="androidx.recyclerview.widget.StaggeredGridLayoutManager"
                app:layout_behavior="@string/appbar_scrolling_view_behavior"
                app:spanCount="@integer/list_items_columns"
                tools:context=".MainActivity"
                tools:listitem="@layout/item_list_content" />

        </FrameLayout>

        <!--<androidx.appcompat.widget.SearchView-->
            <!--android:id="@+id/searchView"-->
            <!--style="@style/Widget.AppCompat.SearchView.ActionBar"-->
            <!--app:iconifiedByDefault="false"-->
            <!--android:layout_marginTop="@dimen/search_view_margin_top"-->
            <!--android:layout_width="match_parent"-->
            <!--android:layout_height="wrap_content">-->

        <!--</androidx.appcompat.widget.SearchView>-->

        <com.lapism.searchview.widget.SearchView
            android:id="@+id/searchView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginLeft="12dp"
            android:layout_marginRight="12dp"
            android:layout_marginTop="@dimen/search_view_margin_top"
            android:background="@android:color/transparent"
            app:layout_behavior="@string/search_behavior"
            app:search_hint="@string/search_view_hint_text"
            app:search_mic_icon="@drawable/baseline_mic_24"
            app:search_shadow_color="@android:color/transparent"
            app:search_shape="rounded"
            app:search_theme="play" />

        <com.google.android.material.bottomappbar.BottomAppBar
            android:id="@+id/bottomAppBar"
            style="@style/Widget.MaterialComponents.BottomAppBar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom|center_horizontal"
            app:backgroundTint="?android:attr/windowBackground"
            app:buttonGravity="top"
            app:elevation="6dp"
            app:fabAlignmentMode="end"
            app:fabCradleRoundedCornerRadius="0dp"
            app:hideOnScroll="true"
            app:layout_dodgeInsetEdges="none" />


        <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:id="@+id/fab"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom|center_horizontal"
            app:elevation="6dp"
            app:fabSize="normal"
            app:layout_anchor="@id/bottomAppBar"
            app:layout_behavior="@string/appbar_scrolling_view_behavior"
            app:srcCompat="@drawable/sharp_add_24" />

    </androidx.coordinatorlayout.widget.CoordinatorLayout>

    <com.google.android.material.navigation.NavigationView
        android:id="@+id/navigationView"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="start|left"
        android:fitsSystemWindows="false"
        app:headerLayout="@layout/navigation_header"
        app:menu="@menu/navigation_menu" />

</androidx.drawerlayout.widget.DrawerLayout>